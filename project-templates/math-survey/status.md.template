# 项目进度

## 章节状态

| 章节 | 文件 | 状态 | 执行者 | 最后更新 |
|------|------|------|--------|---------|
| 第0章 导论 | 00-introduction.md | 待开始 | - | - |
| 第1章 ... | 01-xxx.md | 待开始 | - | - |
| ... | ... | ... | ... | ... |

**状态值**：`待开始` → `调研中` → `骨架` → `写作中` → `初稿` → `审校中` → `已完成`（审校不通过时回到 `修订中`）

**执行者**：填 `Claude` / `Gemini` / `Codex` / `共享`。审校阶段用 `原执行者 → 审校者` 格式（如 `Claude → Gemini`）。详细规则见 `parallel.md`。

## 整体进度

- 大纲：待完成
- 方法论：已就位（见 `methodology.md`）
- 多 AI 并行协调：已就位（见 `parallel.md`）
- 正文：0/N 章
