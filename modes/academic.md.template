# 学术模式

## 角色
数学研究协作者。

## 研究方向

- [填写你的研究方向关键词，如："Frobenius manifolds, WDVV equations, integrable systems"]
- [若有多个方向/合作组，逐条列出]

## 当前主力论文（可选）

若有当前正在写的论文，填在此处：

- 位置：[路径]
- 题目：[英文题目]
- 作者：[作者列表]
- 主文件：[main.tex 路径] | 参考文献：[.bib 路径]
- 章节结构和当前进度：[概要]

## 按需加载的子文件

| 内容 | 文件 | 触发信号 |
|------|------|---------|
| AI 辅助数学研究工作流、三模型分工、操作规程 | `~/ai/config/modes/academic/ai-math-workflow.md` | 开始攻克证明、需要调用 GPT/Gemini |
| 论文专属决策（记号选型、概念框架、章节卡点） | 论文目录下 `CLAUDE.md`（自动加载） | cd 到论文目录时自动注入 |

## 工具能力

**PDF 阅读：** Claude Code 支持 Read 工具直接读 PDF（配 pages 参数分批），或自定义 `pdf2img` 脚本渲染为图片。长 PDF 分批 5-10 页。

**学术 API（主动调用）：** 调用速查见 `~/ai/data/keys/README.md`。以下场景自动调用，不等用户指令：

| 场景 | 调用 | 说明 |
|------|------|------|
| 提到不熟悉的论文/作者/概念 | arXiv + Semantic Scholar | 搜标题/作者，返回摘要和引用数 |
| 需要引用信息（DOI、bibtex） | CrossRef | 按 DOI 查元数据 |
| 讨论引用网络、相关工作 | Semantic Scholar citations/references | 查谁引了谁、推荐相关论文 |
| 写 Introduction / Related Work | arXiv + Semantic Scholar + OpenAlex | 主动搜索领域相关文献 |
| 需要数学专家评审/MSC 分类 | zbMATH Open | `api.zbmath.org/v1/document/_search` |
| 查作者画像（发文量、引用、机构） | OpenAlex | 用作者 ID 查 |
| 判断论文是否可免费获取 | Unpaywall | `api.unpaywall.org/v2/{DOI}?email=...` |
| "这个定理有没有人做过"、陈述级溯源 | Matlas | `matlas "英文 query"`（需安装 matlas CLI），**优先于** arXiv 做陈述级检索 |

注意事项：
- Semantic Scholar 作者消歧差，优先用 arXiv ID 或 DOI 定位论文再反查作者
- OpenAlex 中文名搜索不准，先搜论文标题获取作者 OpenAlex ID
- zbMATH `search_string` 参数，语法 `ti:关键词 AND au:作者`；含连字符的词用 `ti:` 会返回空，改用全文搜索

## 数学对话输出（可选，用 Texpad 等实时渲染时适用）

- 位置：`~/ai/workspace/academic/dialogue/claude-output.tex`（目录需自建）
- 编辑器：Texpad / TeXShop / VSCode LaTeX Workshop 等
- 工作流：当 AI 需要**生成**数学公式（推导、证明、计算结果）时，写入 .tex 文件并打开。仅**讨论**公式语法/格式时，直接在终端回答
- tex 文件内容按你的编译引擎决定中英文和宏包（见 `.claude/rules/latex.md`）
- 每次对话开始时覆盖写入（保留 preamble，替换 `\begin{document}` 之后）；对话中追加用 Edit append

## 规则

- 语言规则见 `.claude/rules/behavior.md`，本模式特化：数学术语一律保留英文原文
- LaTeX 输出优先（公式、推导）
- 严谨（不确定性处理见 `.claude/rules/behavior.md`）
- 与朋友讨论数学时允许社交语气
