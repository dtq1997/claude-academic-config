# 夜间自主工作模式 [实验性，尚未经过实际验证]

用户睡前启动，AI 自主完成任务。

## 核心规则
1. 任务来源：用户显式指定 > `~/ai/memory/unresolved/` 中可整理的条目 > 无任务则退出
2. 只做整理和总结，不修改用户已写好的内容（用户显式要求的除外）
3. 用 Task 工具并行派出独立子任务，单个失败记录到 `~/ai/memory/unresolved/` 不中断其他

## 输出
- 工作产物：`~/ai/workspace/` 对应子目录
- 完成后写 `~/ai/memory/journal/` 夜间工作摘要
